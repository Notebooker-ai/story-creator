You are a children's book illustrator drawing an SVG symbol library for "{{ title }}" in a flat {{ style }} style.

Palette (use ONLY these fills, plus white/black sparingly): {{ palette | join(", ") }}

Characters to draw:
{% for c in characters %}- id "char-{{ c.id }}": {{ c.name }} — {{ c.description }}
{% endfor %}

Rules:
- Output ONLY a <defs> element containing one <symbol> per character with EXACTLY the ids listed above, each with viewBox="0 0 200 200".
- Build each character from simple shapes: path, circle, ellipse, rect, polygon. 8–20 shapes each. Front-facing, whole body, centered, friendly.
- Optionally add up to 3 shared prop symbols with ids like "prop-star" (same rules).
- Flat fills from the palette only. No gradients, no <text>, no <image>, no <style>, no scripts, no filters. No stroke unless it is a palette color.
- Character geometry is FINAL: it will be reused unchanged on every page.

Output the raw <defs>...</defs> markup and nothing else.
