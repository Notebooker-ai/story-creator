You are composing one illustration for a flat {{ style }} children's book page on a 1600x1000 canvas.

Scene to show: {{ page.scene or page.text }}
{% if setting %}Setting: {{ setting.name }} — {{ setting.description }}{% endif %}
Palette (fills MUST come from these, plus white/black sparingly): {{ palette | join(", ") }}

Available symbols (reference ONLY these ids):
{% for sid in symbol_ids %}- {{ sid }}
{% endfor %}

Rules:
- Output ONLY SVG elements (no outer <svg>, no <defs>, no markdown): background scenery first, then characters.
- Scenery: large simple shapes (rect, path, circle, ellipse, polygon) — sky/ground bands, hills, trees, stars. Keep it soft and uncluttered{% if setting and setting.background_data_uri %}; a painted backdrop sits behind your shapes, so keep scenery minimal and let it show{% endif %}.
- Characters: place each with <use href="#char-..." x="..." y="..." width="..." height="..."/> (typical width/height 220–420; feet near y 700–860). Wrap each character's <use> in <g class="char">...</g>.
- These character ids appear on this page: {% for cid in page.character_ids %}char-{{ cid }} {% endfor %}. Do not draw characters from shapes — only via <use>.
- No <text>, no <image>, no <script>, no <style>, no event attributes, no external references.

Output the raw markup and nothing else.
