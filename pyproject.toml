[project]
name = "story-creator"
version = "0.4.0"
license = "MIT"
description = "Open Notebook creator: illustrated stories from notebook sources — picture books, short stories, fables and bedtime stories for any reader from toddlers to adults, with one AI-painted illustration per page, cast-consistent characters, and HTML/EPUB/PDF editions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "open-notebook-creator-sdk>=0.10",
    "esperanto>=2.19",
    "ai-prompter>=0.3.1",
    "langchain-core>=0.3",
    "loguru>=0.7",
    "httpx>=0.27",
    "pillow>=10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.6",
]

[project.entry-points."open_notebook.creators"]
stories = "story_creator:StoryCreator"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/story_creator"]

[tool.uv.sources]
open-notebook-creator-sdk = { git = "https://github.com/Notebooker-ai/open-notebook-creator-sdk.git", rev = "56ea7a674d1632381dde47b71299bd14a12d6035" }

[tool.pytest.ini_options]
asyncio_mode = "auto"
